class PersonController {
    static async getAll (req, res) {
        try {
            //acess model

        } catch (error) {
            //error
        }
    }
}

module.exports = PersonController;